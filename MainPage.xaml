<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:maui_listview_alternating_row_converter"
             x:Class="maui_listview_alternating_row_converter.MainPage"
             Title="Monkeys"
             x:DataType="local:MainPageViewModel">
    <StackLayout Margin="20">
        <ListView
            x:Name="ListView"
            ItemsSource="{Binding Monkeys}" 
            HasUnevenRows="true">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Monkey">
                    <ViewCell>
                        <Grid Padding="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.BackgroundColor>
                                <Binding
                                    Path="."
                                    ConverterParameter="{x:Reference ListView}">
                                    <Binding.Converter>
                                        <local:AlternatingRowColorConverter
                                            PrimaryColor="Primary"
                                            SecondaryColor="Secondary" />
                                    </Binding.Converter>
                                </Binding>
                            </Grid.BackgroundColor>
                            <Image Grid.RowSpan="2" 
                                   Source="{Binding ImageUrl, TargetNullValue={StaticResource fallbackImageUrl}}" 
                                   Aspect="AspectFill" 
                                   HeightRequest="60" 
                                   WidthRequest="60" />
                            <Label Grid.Column="1" 
                                   Text="{Binding Name}" 
                                   Style="{DynamicResource TitleStyle}" />
                            <Label Grid.Row="1" 
                                   Grid.Column="1" 
                                   Text="{Binding Location, TargetNullValue={StaticResource locationUnknown}}" 
                                   VerticalOptions="End" 
                                   Style="{DynamicResource SubtitleTextStyle}" />
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a hovercraft number nine" />

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>
    </StackLayout>
</ContentPage>
